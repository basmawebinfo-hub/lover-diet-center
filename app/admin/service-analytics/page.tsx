import { fallbackBodyServices, fallbackReviews } from "@/lib/body-services"
export default function Page(){const average=(fallbackReviews.reduce((s,r)=>s+r.rating,0)/fallbackReviews.length).toFixed(1);return <div dir="rtl" className="flex flex-col gap-7"><header><h1 className="text-3xl font-black">تحليلات الخدمات</h1><p className="mt-2 text-neutral-500">نظرة سريعة على أداء موديول العناية ونحت الجسم</p></header><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["الخدمات النشطة",fallbackBodyServices.length],["متوسط التقييم",average],["العروض الحالية",fallbackBodyServices.filter(s=>s.compareAtPrice).length],["متوسط سعر الجلسة",`${Math.round(fallbackBodyServices.reduce((s,x)=>s+x.price,0)/fallbackBodyServices.length)} د.إ`]].map(([label,value])=><article key={label} className="rounded-2xl border bg-white p-6"><p className="text-sm font-bold text-neutral-500">{label}</p><strong className="mt-3 block text-3xl font-black text-emerald-800">{value}</strong></article>)}</div><section className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-black">مقارنة أسعار الخدمات</h2><div className="mt-6 flex flex-col gap-4">{fallbackBodyServices.map(s=><div key={s.id} className="grid grid-cols-[minmax(120px,1fr)_3fr_70px] items-center gap-3"><span className="truncate text-sm font-bold">{s.nameAr}</span><div className="h-3 overflow-hidden rounded-full bg-neutral-100"><div className="h-full rounded-full bg-emerald-700" style={{width:`${s.price/3.5}%`}} /></div><span className="text-left text-sm font-bold">{s.price}</span></div>)}</div></section><p className="text-sm text-neutral-500">تظهر مؤشرات الحجوزات والإيرادات تلقائيًا بعد تشغيل ملفات SQL وبدء استقبال الحجوزات.</p></div>}
